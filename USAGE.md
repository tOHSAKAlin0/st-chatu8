# 人物定义与历史绘图提示词参考版使用说明

本修改版基于 st-chatu8 2.8.4。当前版本为 `2.8.4-definitions.3`（人物定义版 D3），包含此前 R2 的历史绘图提示词功能。原作者：从前跟你一样。

## 人物定义参考（D3）

D3 在 **LLM → 上下文历史** 新增“参考人物定义楼层数”和“查看人物定义”。更新后刷新页面，界面标识应为“人物定义版 D3”。

- 默认自动引用目标楼层之前最近 **3 个有有效人物定义的楼层**，范围 0–20。跳过无定义和已失效的记录；不引用当前及后续楼层。0 关闭引用，仍保存新定义。
- 参考窗口内按人物 `id` 自动合并：相同编号只传入最后一个有效楼层的完整定义，六个字段整体采用最新记录，不拼接旧字段。不同编号保留；同名但编号不同不会合并。各楼层原始记录仍可查看，合并后不会额外向窗口之外补取人物。
- Agent 返回有效人物定义和图片方案后，定义随当前聊天及 swipe 保存；NovelAI 随后出图失败也可复用。支持现有 v2/v3/漫画预设，无需重新导入。
- 自定义预设可用 `{{历史人物定义}}` 指定插入位置；没有变量时自动附加。预设需输出 `definitions/characters`，人物字段为 `id`、`name`、`basis`、`appearance`、`outfit`、`continuity`；旧图片不会自动补成完整定义。
- 在“组合提示词”核对引用楼层、人数与注入方式。在“查看人物定义”查看所有已保存版本、删除某条记录或清空当前聊天；删除前会确认，不影响聊天和图片。
- 同层重画替换该版本最近一次有效定义。正文或前序剧情改变后，旧记录会停止引用；请求期间切换聊天、swipe 或修改正文会阻止结果写回。

目前只支持普通聊天正文生图及人物定义，不保存场景或物件，不建立全局人物身份库。历史人物定义与历史绘图提示词是两个独立选项，窗口以外的旧人物不会额外补发。手动删除记录时，当前聊天未完成的绘图 Agent 请求也会取消写回。

本地基础检查通过；实际云酒馆保存、刷新重载和图片连续性仍需试用。首次生图后先刷新查看人物定义是否保留，再在下一楼层检查组合提示词。

## 安装与更新

在云酒馆或 SillyTavern 的扩展管理器中填写以下地址安装：

```text
https://github.com/tOHSAKAlin0/st-chatu8
```

原版与修改版同名，替换前先导出插件设置、LLM 上下文预设并备份重要聊天。仅关闭原版不会移除目录；如果平台支持更换仓库来源，改为上述地址后更新，否则备份后卸载原版再安装。各平台卸载时是否保留设置可能不同。

已经安装本仓库后，通过扩展管理器更新并刷新页面即可。标题和 LLM 页顶部出现「人物定义版 D3」表示新界面已加载。

## 开启历史提示词参考

1. 下载并在插件的 **LLM → 上下文预设** 中导入 [v3 历史提示词预设](presets/novelai-v5-story-agent-history.json)。
2. 在 **请求类型配置 → 正文图片生成** 中绑定 `NovelAI V5 · 剧情插图 Agent v3 · 历史提示词`，选择自己的文字 LLM API 配置。
3. 在 **LLM → 上下文历史** 中，把「参考历史绘图楼层数」先设为 **1**。该数字输入框位于「发送历史层数」下方；设为 **0** 关闭，最多 **20**。
4. 正常触发正文生图，在「组合提示词」中检查引用的历史提示词及来源楼层。

更新插件不会自动替换已保存的预设，需要单独导入和绑定。自定义预设可通过 `{{历史绘图提示词}}` 接收历史内容；未包含该变量的旧预设保持原行为。

## 使用范围

### 可选：彩色无字漫画预设

下载 [剧情漫画 Agent v1 · 历史提示词](presets/novelai-v5-story-comic-history.json)，在 **LLM → 上下文预设** 导入，再在 **请求类型配置 → 正文图片生成** 中绑定 `NovelAI V5 · 剧情漫画 Agent v1 · 历史提示词`，选择自己的文字 LLM API。生图后端使用 NovelAI V5。

该预设自动选择 **1–3 页，每页 2–4 格**；一张生成图片就是一整页彩色无字漫画，按从左到右、从上到下阅读。两格上下排列，三格上方一宽格、下方两格，四格两行两列。漫画通过动作和表情叙事，不生成对白、旁白、气泡、拟声文字、标题或页码，原聊天正文保留文字内容。

人物与场景先做详细定义。角色栏只写身份、外貌和稳定衣着等人物信息；动作、姿态、视线、位置关系、表情与换装状态直接自然写进总栏 `scene_composition` 的对应 `Panel` 描述，每格只在开头标记一次格号。每页最多四个独立角色，同一人物跨格复用一个角色块。页数按剧情选择，正文只有一个可用定位行时会压缩到一页并重选关键镜头；不会为增加页数重复插入到同一行。可在本次用户需求中指定允许范围内的页数、格数和叙事重点。

历史参考仍通过「参考历史绘图楼层数」开启，建议先设为 **1**。既能参考旧单幅插图，也能按格序参考旧漫画提示词；这不等于保存完整角色定义。无历史时仍可正常使用。

导入后检查 NovelAI 面板固定正负提示词：禁止漫画、禁止分格或强制黑白的内容可能抵消本预设，例如负面中的 `comic`、`multiple panels` 或正面中的 `monochrome`。按需手动调整；预设不会自动修改这些配置，也不改变尺寸、采样步数或其他生图参数。较小画布中的多格细节效果需要自行试用判断。

漫画版采用独立名称，旧 v2/v3 插图预设保留；需要切回时重新绑定旧预设即可。再次导入同名漫画版会覆盖其条目，自行改过请先导出备份。导入新预设不会自动切换请求绑定。

本版已做格式与示例的静态检查，尚未验证真实文字模型和 NovelAI 出图效果。试用重点：分格数量、阅读顺序、人物是否出现在正确的格、是否混合前后动作与衣着、是否出现不需要的文字。尤其需要检验总栏各 Panel 的动作与位置是否对应到正确的角色。

### 历史参考的支持范围

- 目前用于普通聊天的「正文图片生成」，引用当前目标楼层之前、当前 swipe 中保存的有效图片提示词。
- 数量按有图楼层计算，跳过无有效记录的楼层；每层引用全部有效提示词，不包含当前或后续楼层。
- 这是对已有提示词的参考，不是完整角色记忆库。人物、场景细节能否延续仍取决于历史记录及模型回复。
- 无法关联楼层的前端卡图片组不在支持范围内；没有有效历史时仍可正常生图。
- 原有「历史正文保留 image 标签」是独立设置，使用新预设通常无需再开启。

## 常见问题

**提示人物定义保存未确认：** 先更新到 D3 并刷新。D3 按字段内容核对保存结果，不受服务端 JSON 字段排序影响；读到旧记录时短暂重试，最多读取三次。如果仍失败，提示会区分消息主记录、swipe 副本缺失或具体不一致字段。保存异常时原记录保留，不声称新定义已保存；请提供完整提示以进一步排查实际平台行为。

**没有看到新设置：** 确认安装来源为上述仓库，在扩展管理器更新并刷新页面，然后检查「人物定义版 D3」标识。未导入预设也应显示该设置，不需要先清空个人配置。

**设置出现但没有引用历史：** 检查参考数量是否大于 0、正文生图是否绑定 v3 预设，以及此前楼层是否保存了有效绘图提示词。

**重装后原 API 设置仍在：** 酒馆可能保留用户配置，这是正常现象。公开插件仓库不会自动公开你在酒馆中填写的 API 设置。

**想关闭历史绘图提示词参考：** 将对应参考数量设为 0，或切回[原 v2 预设](presets/novelai-v5-story-agent.json)。D3 的人物定义参考独立控制，需将“参考人物定义楼层数”设为 0；切换预设不会关闭自动注入。
